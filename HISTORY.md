# 개발 히스토리 (전체)

프로젝트 시작부터 지금까지의 전체 기록. `git log` 커밋 메시지를 근거 자료로 삼아
정리했고, 최근 두 커밋(2026-08-04, 2026-08-05)은 실제 대화 세션에서 있었던
디버깅 과정·사용자 요청까지 기억하고 있어서 훨씬 상세하게 적었어.

**주의**: 앞의 5개 커밋(2026-08-01)은 그 작업이 이뤄진 대화 세션 자체를 내가
기억하고 있는 게 아니라, 커밋 메시지에 이미 상세히 남아있는 내용을 그대로
정리한 것. 커밋 메시지에 없는 세부 논의(왜 그 방식을 택했는지 등)까지는
복원할 수 없다는 점 참고.

---

## 1. 초기 커밋 — 프로젝트 뼈대 (2026-08-01)

`43b64c3` Initial commit: math problem extraction app

- PDF를 업로드하면 문제/수식을 인식하고 LaTeX OCR 결과를 검토하는 Flask
  백엔드 + Vue 3 프론트엔드로 시작.

---

## 2. 리뷰 페이지 슬라이드 뷰 + UX 개선 (2026-08-01)

`2791df9` Improve problem review page: per-problem slide view and UX fixes

- 페이지 전체를 50/50으로 나눠 스크롤하던 방식 → 문제 단위로 이미지가
  확대/이동되는 슬라이드(캐러셀) 뷰로 교체. 수식 영역을 드래그로 정확히
  잡을 수 있을 만큼 커짐.
- 좌우 방향키로 문제 간 이동 (텍스트 입력 중에는 비활성화).
- 버그 수정: 이전 리전 조정이 저장 중일 때 새로 드래그하면 요청이 겹쳐
  전송되어 UI와 서버 상태가 어긋나던 문제 — "Saving..." 상태 표시 + 동시
  드래그 차단으로 해결.
- Prev/Next를 화면 하단 고정 플로팅 바로 이동, Confirm 버튼 우측 정렬.
- LaTeX 소스 textarea와 렌더링 미리보기 박스 확대.

---

## 3. 실험: MathLive로 LaTeX를 렌더링된 형태에서 직접 편집 (2026-08-01)

`f82ac26` Experiment: edit LaTeX directly through its rendered form via MathLive

- 원본 텍스트 textarea + 읽기전용 KaTeX 미리보기 조합을, 렌더링된 형태
  그대로 편집 가능한 MathLive `<math-field>`로 교체하는 실험.
- 알려진 트레이드오프: 분수·극한·위첨자 같은 단순 수식은 편집이 깔끔하지만,
  `\begin{array}` 기반의 조건별 정의(케이스) 수식은 행간이 압축되고
  들쭉날쭉하게 렌더링됨 — 기존 KaTeX 출력과 비교해보고 채택 여부 결정
  필요하다는 메모가 커밋 메시지에 남아있음.
- (이 math-field 방식은 이후 커밋들에서 계속 쓰이고 있어 최종적으로 채택된
  것으로 보임.)

---

## 4. 구조화된 문제 콘텐츠 + 크기 조절 가능한 리뷰 레이아웃 (2026-08-01)

`f6c645b` Add structured problem content and resizable review layout

**콘텐츠 모델**
- `ProblemContent`에 `label`(①, ㄱ, 보기 같은 표시 마커)과
  `parent_content_id`(자기참조, `<보기>`나 (가)/(나) 조건 목록 같은 박스형
  그룹을 위함) 추가.
- 객관식 선지(①②③④⑤) 인식을 두 가지 방식으로: PDF 텍스트 레이어의 폰트가
  제대로 인코딩돼 있으면 텍스트에서 바로 추출, 아니면 pix2text 결과에서
  반복되는 `\oplus` 마커를 인식해 나중에 분리 (일부 PDF 폰트는 원 숫자를
  실제 유니코드가 아닌 엉뚱한 글리프로 매핑해두기 때문).
- 연속된 선지 2개 이상은 인식 시점에 자동으로 그룹으로 묶임 (수동 작업 불필요).
- 자동 인식이 못 하는 경우를 위한 수동 대응 수단: 행을 선택해서 원하는
  라벨로 그룹핑, 그룹 해제, 빈 텍스트/수식 행 추가(수식 행은 바로
  Adjust-region 그리기 모드로 진입), 행 라벨 직접 편집.
- 선지 행도 formula 행과 같은 MathLive math-field로 렌더링 (어차피 실제
  LaTeX 콘텐츠이므로).

**리뷰 페이지**
- `ContentRow.vue`를 분리해서 최상위/그룹 내부 두 깊이에서 동일한 행
  마크업을 재사용.
- 이미지 패널과 결과 패널 사이에 드래그 가능한 구분선 추가 (기존 고정
  65/35 비율 대체).
- 버그 수정: 이미지의 실제-표시 배율(오버레이 박스 위치 계산의 기준값)이
  최초 이미지 로드 시점에만 계산되어, 패널 크기를 조절하면 오버레이가
  어긋나던 문제 — 줌/크롭 계산에 쓰이던 동일 ResizeObserver에서 재계산하도록
  수정.

---

## 5. 스캔 PDF 지원, 이미지 콘텐츠 블록, 선지 인식 개선 (2026-08-01)

`0f82283` Support scanned PDFs, image content blocks, and choice detection improvements

**스캔 PDF 인식 (`backend/app.py`)**
- `ProblemContent.crop_path`를 이미지 타입 콘텐츠용으로 추가.
- 새 함수 `recognize_scanned_problem_region`: 페이지에 PDF 텍스트 레이어가
  전혀 없는 경우(스캔 문서), pix2text의 텍스트+수식 통합 페이지 분석으로
  레이아웃과 수식 콘텐츠를 가져오고, 감지된 각 텍스트 영역은 EasyOCR
  (한국어+영어)로 다시 읽음 — pix2text 자체 텍스트 인식이 한글에 맞춰져
  있지 않기 때문.
- `extract_choice_labels`가 이제 텍스트 타입 세그먼트도 확인 — PDF 텍스트
  레이어 기반 문자 분류기와 달리 pix2text는 원 숫자 마커를 "formula"가
  아니라 "text"로 분류하기 때문.
- `merge_empty_choice_markers`/`relabel_choice_row`: pix2text가 원 숫자
  마커를 관계없고 일관성도 없는 기호로 잘못 읽는 경우가 많음 (①과 ⑤가
  둘 다 "®"로 나오기도 함). 한 행에서 마커 하나라도 제대로 읽히면, 나머지는
  개별 인식 결과를 믿는 대신 좌→우 위치 기준으로 ①②③...으로 재라벨링.

**수동 콘텐츠 블록 (`backend/app.py`, 프론트엔드)**
- 새 콘텐츠 타입 "image": 별도 인식 없이 자르기만 해서 그대로 보여주는
  영역 — OCR이 불가능한 다이어그램/그래프용.
- "+ Formula" / "+ Text" / "+ Choice" / "+ Image" 버튼으로 빈 블록을 수동
  추가하고 Adjust Region으로 배치 — 자동 인식이 틀리거나 놓친 부분 대응.
- 이미지 영역을 지정하면 그 영역이 상당 부분(70% 이상) 겹치는 다른 콘텐츠
  블록은 삭제 — 다이어그램 자체의 축 라벨/좌표가 별도의 중복 텍스트/수식
  조각으로 따로 인식되는 걸 방지.
- 그룹(박스형 섹션)도 다른 최상위 항목처럼 순서 변경 가능해짐. 단, 자동
  생성되는 선지 그룹은 항상 맨 마지막에 오도록 서버에서도 강제.

**리뷰 페이지**
- 새 한국어 사용법 가이드(`ReviewGuide.vue`) 추가, 리뷰 페이지 헤더에서
  링크.

---

## 6. 교차 문서 큐, 인쇄/미리보기 시스템, 수식 표시 옵션 (2026-08-04)

`54a3863` Add cross-document queues, print/preview system, and formula display controls

이 커밋부터는 실제 대화 세션 기억을 바탕으로 상세히 적음.

**교차 문서 작업 큐**
- 기존에는 문서를 하나씩 열어서 페이지별로 영역 지정 → 다시 열어서 페이지별
  리뷰, 순서로 작업해야 했음. 여러 문서를 업로드한 뒤 "아직 안 끝난 것"을
  찾으려면 매번 문서를 눌러봐야 하는 게 불편하다는 요청으로 시작.
- **Region Queue**: 아직 영역 지정이 안 된 페이지를 모든 문서에 걸쳐
  모아서 보여주는 페이지.
- **Review Queue**: 영역 지정은 끝났지만 리뷰(OCR 결과 확인)가 안 끝난
  페이지를 모아서 보여주는 페이지.
- 기존 문서별 흐름(Documents → 문서 열기 → 페이지 선택)은 그대로 유지,
  큐는 추가 경로로 제공.
- `RegionEditor.vue`를 `PageViewer.vue`에서 분리해 큐 페이지에서도 재사용.
- `App.vue`에 상단 고정 탭 바(Documents / Region Queue (N) / Review Queue
  (N)) 추가, 실시간 대기 건수 표시.

**인쇄/미리보기 시스템**
- 실제 한국 모의고사와 비슷한 형태로 저장된 문제들을 인쇄해볼 수 있는
  기능 요청. 폰트는 "신명중명조"로, A4 기준 세로 2단 분할로 시작.
- `ProblemPrintout.vue`를 렌더링 핵심 로직으로 분리 — 전체 문서 인쇄 뷰
  (`PrintPreview.vue`)와 리뷰 페이지 내 개별 문제 실시간 미리보기 양쪽에서
  공유.
- 선지 3/2/1단 배치를 선지 길이 기준으로 적응형으로 조정 (LaTeX 마크업을
  제외한 "시각적 길이" 추정치 사용 — `\sqrt{3}`처럼 마크업이 많은 짧은
  수식이 실제 폭보다 과대평가되는 문제 수정).
- 미리보기를 리뷰 페이지의 review-content 박스 바깥, 별도의 항상 보이는
  박스로 분리 배치 (수정 → 실시간 반영).
- OCR 텍스트 정리 규칙 추가: 덩어리(텍스트/수식) 앞뒤 공백 제거, 덩어리
  사이에는 공백 삽입하되 수식 바로 뒤에 오는 텍스트는 공백 생략 (조사가
  수식에 바로 붙는 한국어 특성 반영).

**수식 표시 옵션 (`display_mode`)**
- 수식마다 인라인/한 줄 전체 차지를 선택할 수 있는 체크박스 추가, DB에
  영속화되어 인쇄 시에도 그대로 적용.
- 체크 해제(인라인) 상태에서도 폰트 크기는 원본 크기를 유지하도록 —
  LaTeX 소스 자체에 `\displaystyle`을 붙이되 KaTeX의 `displayMode` 옵션은
  false로 유지하는 방식 (그러면 줄바꿈 없이 인라인 유지하면서 크기만
  풀사이즈).

**KaTeX 렌더링 버그 수정**
- 케이스(조건별 정의) 블록 안의 분수가 다른 항목보다 작게 보이는 문제 —
  `array`/`cases` 환경이 자기 행의 수식 스타일을 강제로 압축시키는 특성
  때문. `\frac`를 `\dfrac`으로 치환해 해결.
- 선지 안의 수식도 본문과 동일한 `\displaystyle` + dfrac 처리 통일.

---

## 7. 선지/케이스 수식 렌더링, 보기 그룹 스타일·줄바꿈, OCR 되돌리기, 점 아티팩트 정리 (2026-08-05)

`e2a88fd` Fix choice/cases formula rendering, add bogi group print style and line-break controls, OCR revert snapshot, and stray-dot cleanup

자세한 내용은 `fix.md` 참고. 요약:

- 6번 커밋에서 놓쳤던 케이스 블록 감지 범위 확장(`\begin{array}`도 인식)과
  행간(`\arraystretch`) 조정.
- 실제 설치된 한글 폰트 `(한)신중명조`로 인쇄 폰트 교체 (기존 지정 폰트들은
  시스템에 없거나 Chromium이 로드하지 못하는 상태였음 — GDI+/DirectWrite
  차이로 진단).
- `<보기>` 그룹 전용 인쇄 스타일(위아래 가로선 + 중앙 라벨) 추가.
- 그룹 내부 콘텐츠가 OCR 조각마다 강제 줄바꿈되던 문제 수정 → 본문처럼
  자연스럽게 흐르도록.
- 그룹 내부 줄바꿈을 원하는 위치에 수동 지정할 수 있는 체크박스 + "보기"
  그룹에서 (가)/(나)/ㄱㄴㄷ 패턴을 자동 감지해 줄바꿈하는 휴리스틱 추가.
- 문제 본문↔선지, 선지 행 간 간격 확대.
- 리뷰 중 저장 전 수정한 값이 새 블록 추가 시 초기화되던 버그 수정.
- "초기 OCR 결과로 되돌리기" 기능 추가 — 재인식(OCR 재호출)과는 별개로,
  최초 인식 시점의 스냅샷을 즉시(모델 재호출 없이) 복원.
- 프라임(도함수 `'`) 표기의 이중 중첩 위첨자, `\stackrel{.}{X}` 오사용
  두 가지 "글자 위 점" OCR 아티팩트 정리.

---

## 8. 수식 기본값, 페이지/문서 갤러리 뷰, 드래그·포커스 기반 UX 개선 (2026-08-06)

`3f44765` Default formulas to inline, add page/document gallery views, and improve drag/focus-driven review UX

`e2a88fd` 이후 진행한 작업들.

**수식 "Full line" 체크박스 기본값**
- 요청: 길이/복잡도와 무관하게 항상 기본 해제(인라인) 상태로.
- `_is_complex_formula`/`_COMPLEX_FORMULA_RE` 휴리스틱(`\lim`/`\sum`/`\int`/
  `\prod`/`\begin{cases}` 포함 여부나 24자 초과 시 자동 체크)이 이제 유일한
  호출부였던 `display_mode` 시딩 로직에서 빠지면서 완전히 죽은 코드가 되어
  통째로 제거. 새로 인식되는 모든 수식은 `display_mode=False`로 시작.
- 기존에 이미 저장된 행의 `display_mode` 값은 건드리지 않음 (요청 범위가
  "기본값"에 한정됨).

**선지(Choices) 수식 폰트 크기 불일치**
- 원인: KaTeX 자체 CSS(`.katex { font-size: 1.21em; ... }`, 자체 폰트
  메트릭 보정용 기본값)를 다른 모든 곳에서는 `.inline-content
  :deep(.katex) { font-size: 1em; }`로 되돌려놓고 있었는데, 선지 콘텐츠를
  감싸는 span에만 `inline-content` 클래스가 누락되어 있었음.
- 수정: 해당 span에 `class="inline-content"` 추가. 본문/선지 KaTeX
  font-size가 정확히 일치하는 것 확인 (14px = 14px).

**문서 내 페이지 목록 — 갤러리 뷰 (완료된 문서 한정)**
- 요청 배경: 영역 지정이 다 끝난 여러 페이지짜리 문서에서, 좁은 세로
  리스트로 페이지를 고르기 불편함.
- `PageViewer.vue`: 문서의 `status === 'selection_completed'`일 때만
  페이지 사이드바가 실제 페이지 썸네일이 있는 그리드 갤러리로 전환 (파일명
  대신 이미지 + Page N + 리뷰 상태 아이콘). 아직 영역 지정이 안 끝난
  문서는 기존 좁은 리스트 그대로 유지 — 순서대로 작업하는 흐름과 더
  잘 맞음.

**Tab 키로 콘텐츠 필드 간 바로 이동**
- 문제: 리뷰 중 Tab을 누르면 라벨 입력창, 타입 드롭다운, 체크박스, 버튼
  등을 하나하나 거쳐야 다음 블록의 본문 입력창에 도달함.
- `ContentRow.vue`의 textarea/math-field에 `row-content-field` 클래스를
  붙이고, Tab keydown 시 `document.querySelectorAll('.row-content-field')`
  배열에서 현재 요소의 다음(Shift+Tab이면 이전) 위치로 바로 포커스 이동.
  목록의 처음/끝에서는 기본 브라우저 Tab 동작으로 자연스럽게 넘어감.
  최상위/그룹/선지 그리드 어디서든 동일하게 동작 (같은 컴포넌트 재사용).

**"다음 미검토 페이지로 자동 이동" 시 헤더 안 보이게 스크롤**
- 요청: 한 페이지 검토를 끝내고 자동으로 다음 페이지로 넘어갈 때, 앱
  헤더/탭/브레드크럼이 스크롤되어 안 보이고 바로 3단 리뷰 영역이 보이길
  원함.
- 처음 시도(`scrollIntoView` 즉시 호출)는 두 가지 이유로 실패:
  1. 이미지가 아직 로딩되기 전이라 레이아웃이 확정되지 않은 상태에서
     스크롤 목표를 계산함 → `<img>`의 실제 `load` 이벤트 이후, 거기서
     트리거되는 스케일 재계산까지 `nextTick`으로 기다린 뒤 스크롤하도록
     수정.
  2. 그래도 목표 위치까지 도달하지 못함 — `.review-panel`이 각자
     `max-height: 68vh`로 자체 스크롤하기 때문에 페이지 전체 높이가
     뷰포트보다 아주 조금만 커서, 헤더 전체를 넘길 만큼 스크롤할 여유
     공간 자체가 물리적으로 없었음 (`document.documentElement.scrollHeight
     - window.innerHeight`가 143px뿐이었는데 헤더는 약 285px). `.review-body`에
     `min-height: 100vh`를 줘서 항상 충분한 스크롤 여유를 확보 (패널
     자체 높이는 `max-height`가 그대로 상한선 역할을 해서 변하지 않음).
- 일반 페이지 진입(자동 이동이 아닌 경우)은 영향 없음 — `scrollY: 0`
  그대로, 눈에 띄는 레이아웃 변화도 없음을 확인.

**드래그 중 위쪽 자동 스크롤 — 조사만 하고 코드 변경 없음**
- "블럭 드래그 중 위쪽 방향 자동 스크롤이 안 된다"는 리포트를 받고 코드
  검토 및 재현 시도. 합성 드래그 이벤트로는 정상 동작 확인됐고, 사용자도
  직접 재확인 후 정상 작동한다고 확인 — 실제 버그는 아니었던 것으로
  결론.

**새 블록 삽입 위치 — 마지막 포커스한 입력창 기준**
- 요청: "+ Formula"/"+ Text" 등으로 새 블록을 만들 때 항상 맨 밑에
  추가되는 대신, 지금 보고 있는(작업 중인) 위치에 삽입되길 원함. "지금
  보이고 있는 위치"를 어떤 기준으로 판단할지 물어봤고, 사용자가 "마지막
  으로 포커스한 입력창" 선택.
- `ProblemReview.vue`: 콘텐츠 필드에 포커스가 갈 때마다(`focusin`) 그
  행의 최상위 조상 id를 `lastFocusedContentId`로 기록 (그룹/선지 안의
  필드라면 그 그룹 자체를 앵커로 사용 — 새 블록은 항상 최상위에만
  만들어지므로).
- 백엔드 `create_problem_content`가 `after_content_id`를 받아, 대상의
  `order_index + 0.5`라는 소수 값으로 새 행을 만든 뒤 기존
  `_renumber_siblings`로 깔끔한 정수로 재정렬 — 선지 그룹이 항상 맨
  마지막에 오는 규칙과 자연스럽게 공존 (선지 필드에 포커스가 있었다면
  "그룹 뒤에 삽입"이 정렬 규칙에 의해 자동으로 "그룹 바로 앞"이 됨).
- 실제 데이터로 두 시나리오 모두 확인: 목록 중간의 수식 행에 포커스 후
  삽입 → 그 바로 다음에 위치, 선지 필드에 포커스 후 삽입 → 선지 그룹
  바로 앞에 위치.

**메인 문서 목록 — 리스트/갤러리 전환 토글**
- 요청: 문서 내 페이지 갤러리에 이어, 메인 문서 목록 화면에도 썸네일
  갤러리 보기를 추가하고 사용자가 직접 리스트/갤러리를 선택할 수 있게.
  (문서 내 페이지 목록은 자동 전환 그대로 유지하기로 확인.)
- 백엔드 `document_to_dict`에 `thumbnail_url`(문서 첫 페이지 이미지) 추가.
- `DocumentList.vue`: `viewMode`(list/gallery) 토글 추가, 갤러리 모드는
  각 문서를 표지 썸네일 + 파일명 + 페이지 수 + 상태 배지 카드로 그리드
  표시.
- 처음엔 List/Gallery를 각각 테두리 있는 독립 버튼으로 구현했으나,
  "토글 형태로 디자인해볼 수 있어?"라는 피드백을 받고 세그먼트 컨트롤
  스타일(회색 알약형 트랙 + 활성 항목에 흰색 칩 배경)로 재디자인.

---

## 9. uploads 폴더 직접 추가 자동 처리, 한글 파일명 보존, 문서 이름 수정 (2026-08-07)

`32d42fb` Support scanned PDFs dropped directly into uploads/, preserve Korean filenames, and add document rename

**한글 파일명이 깨지고 있던 원인 발견**
- 업로드 시 표시용 파일명에 `secure_filename()`을 적용하고 있었는데, 이
  함수는 비-ASCII 문자를 통째로 제거함 — 직접 테스트해보니
  `'수학문제.pdf'` → `'pdf'`로 한글이 전부 사라지는 것 확인. 실제 파일은
  항상 UUID 폴더 밑 `original.pdf`로 저장되고 사용자 파일명은 표시용
  문자열일 뿐이라, `secure_filename`이 막아주는 경로 조작 문제는 애초에
  해당되지 않았음 — 그냥 한글만 파괴하고 있었던 것. DB에 있던
  `k_6__.pdf` 같은 파일명도 원래 한글 제목이었다가 이렇게 날아간 결과로
  추정.
- `_safe_display_filename()`으로 교체: 한글을 포함한 원본 파일명을
  그대로 보존하고, U+FFFD(대체 문자)가 섞여 있는 등 이미 인코딩이
  손상된 게 명백한 경우에만 랜덤 이름(`document_xxxxxxxx.pdf`)으로
  대체. (뒤섞인 인코딩처럼 미묘하게 잘못된 경우까지는 감지 못 함 — 이건
  근본적으로 판별이 어려운 문제라 한계로 남김.)

**uploads 폴더에 파일을 직접 추가하면 자동으로 문제 추출 진행**
- 요청: 웹 업로드를 거치지 않고 uploads 폴더에 PDF 파일을 직접 넣어도
  똑같은 처리(페이지 렌더링, 문제 추출 준비)가 이루어지도록.
- 웹 업로드와 직접 추가가 공유하는 `_create_document_from_pdf` 헬퍼로
  기존 업로드 로직을 리팩터링 (Document 행 생성 → 자기 UUID 폴더 생성 →
  PDF 저장 → 페이지별 PNG 렌더링 → Page 행 생성, 이 전체를 하나의 콜백
  기반 함수로 통합, 콜백은 "PDF 바이트를 그 경로에 어떻게 가져다 놓을지"만
  다르게 함 — 웹 업로드는 `FileStorage.save()`, 폴더 스캔은
  `shutil.move()`).
- `_scan_uploads_folder()`: uploads 폴더 최상위에 놓인(=아직 자기
  UUID 폴더 안에 들어가지 않은) `.pdf` 파일을 찾아 위 헬퍼로 처리한 뒤
  자기 UUID 폴더로 옮김. 기존 문서들은 전부 UUID 폴더 안에만 파일이
  있으므로, 최상위에 파일이 있다는 것 자체가 "아직 처리 안 된 새 파일"이라는
  뜻 — 판별 로직이 따로 필요 없음.
- 이 스캔을 `GET /api/documents`에 끼워넣어, 문서 목록 화면을 열거나
  새로고침할 때마다 자동으로 새 파일을 인식하도록 함 (디렉터리 목록
  조회 자체는 가벼워서, 평소엔 처리할 새 파일이 없는 호출에서도 비용이
  거의 없음).
- 실제 검증: 기존 PDF를 `수학_테스트_문서.pdf`라는 이름으로 uploads
  폴더에 직접 복사 → 문서 목록 새로고침 → 자동 인식되어 16페이지 전부
  렌더링, 자기 UUID 폴더로 이동, 최상위에는 파일이 남지 않음. 브라우저
  화면에 파일명이 완벽한 한글로 표시되는 것 확인 (터미널에 찍히는
  `?????`는 Windows 콘솔 코드페이지 표시 문제일 뿐 — 실제 저장된
  문자열이 바이트 단위로 정확히 일치하는 것도 별도로 확인).

**메인 문서 목록에서 이름 수정**
- 요청: 업로드한 문서를 효율적으로 관리하기 위해 메인 페이지에서 이름을
  바로 수정할 수 있게.
- 백엔드에 `PATCH /api/documents/<id>` 추가 — 파일명 문자열만 바꾸는
  순수 DB 업데이트, 실제 파일 경로는 건드리지 않음.
- `DocumentList.vue`: 리스트/갤러리 양쪽 모드에 "Rename" 버튼 추가.
  누르면 파일명이 인라인 입력창으로 바뀌고 Save/Cancel(또는
  Enter/Escape)로 확정·취소. 이름 수정 중에는 행 클릭으로 문서가
  열리는 동작을 막아, 입력 중 실수로 다른 곳을 눌러도 엉뚱한 문서로
  이동하지 않도록 함.
- 실제 UI에서 이름 변경 전/후 값을 비교해 끝까지 확인.

---

## 10. 페이지 단위 즉시 OCR + 순차 큐 설계, 동시 처리 실험 (2026-08-07)

`2b999ee` Trigger OCR per-page immediately, and replace unbounded background threads with a sequential recovery-capable queue

**문서 전체 완료를 기다리지 않고 페이지별로 즉시 OCR**
- 요청 배경: 지금 프로그램은 페이지 하나를 작업 단위로 굴러가는데, 최초
  영역 설정 시에는 문서 전체가 끝나야 OCR이 시작되는 게 이상하다는 지적.
  빠른 결과를 위해 페이지 하나의 영역 선택이 끝날 때마다 바로 OCR을
  넣도록 요청.
- 기존 `save_page_problems`는 `remaining == 0`(문서의 모든 페이지가
  완료)일 때만 인식 스레드를 띄우고, 그마저도 문서 전체의 모든 문제를
  한 번에 처리했음.
- 페이지가 완료될 때마다 그 페이지의 문제만 바로 인식 대상으로 넣도록
  변경. `document.status`(전체 완료 여부, 페이지 갤러리 뷰 등에 쓰임)는
  그대로 유지하되 OCR 실행 시점과는 완전히 분리.
- 실제 데이터로 확인: 16페이지 문서에서 1페이지만 영역 지정 → 응답은
  0.6초 만에 돌아오고, 약 70초 뒤 그 페이지만 `recognized`로 바뀌는 동안
  나머지 15페이지는 전혀 손대지 않은 채 `pending`으로 남아있는 것 확인.

**무제한 병렬 스레드 → 순차 워커 큐로 교체**
- 페이지마다 `threading.Thread(...).start()`를 무제한으로 띄우는 기존
  구조를 그대로 두면, 짧은 시간에 여러 페이지를 완료할 때 OCR이 전부
  동시에 병렬로 돌게 됨 — "1000개를 한꺼번에 넣으면 어떻게 되냐"는 질문에
  답하다가 발견한 구조적 문제.
- `queue.Queue()` 기반 단일 상시 워커 스레드로 교체. 페이지 완료 시
  스레드를 새로 띄우는 대신 큐에 작업을 넣기만 함 (`_enqueue_ocr_task`).
  "Adjust region" 재인식(`update_content_region`)도 같은 큐/워커로 통합 —
  모델 호출이 전부 하나의 스트림으로 순차 처리되도록.
- 같은 페이지/블록이 대기 중이거나 처리 중일 때 다시 큐에 넣으려 하면
  무시하는 중복 방지(`_ocr_queued_keys`) 추가.
- 워커 루프 자체는 개별 작업의 예외를 잡아서, 작업 하나가 실패해도 뒤에
  줄 서 있는 나머지가 멈추지 않도록 함.
- **재시작 복구**: `Problem.status`가 OCR 성공 전까지 DB에 계속
  `pending`으로 남아있다는 점을 이용해, 프로세스 시작 시
  `_recover_ocr_queue_on_startup()`이 아직 처리 안 된 페이지를 전부
  다시 큐에 넣음 — 서버가 크래시하거나(혹은 개발 중 reload) 재시작돼도
  대기 중이던 작업이 유실되지 않고 자동으로 이어짐. Flask 디버그
  리로더가 프로세스를 두 번 띄우는 특성 때문에, 실제 서빙 프로세스에서만
  (`WERKZEUG_RUN_MAIN`) 워커/복구를 시작하도록 가드.
- 실제로 검증: 6개 페이지를 연달아 큐에 넣고 0.5초 간격으로 폴링해서
  정확히 한 번에 하나씩만 완료되는 것 확인(1→2→3 순서로 겹치지 않게
  전환). 이어서 다시 6개를 큐에 넣은 직후 python.exe 프로세스를 강제
  종료해서 전부 처리 중간에 끊기게 만들고(6개 다 `pending`으로 남은 것
  확인), 서버를 재시작하니 사용자 개입 없이 전부 자동으로 이어서
  처리되어 `recognized`로 바뀌는 것까지 확인.
- 한계로 남겨둔 것: 어떤 페이지의 OCR이 큐에 대기 중이 아니라 이미
  실행되고 있는 도중에 같은 페이지를 다시 완료 처리하면, 그 순간 기존
  Problem 행이 삭제되면서 처리 중이던 워커와 레이스 컨디션이 생길 수
  있음 — 발생 가능성은 낮지만 명시적으로 막아두지는 않음.

**동시 처리(워커 2개) 실험 — 실측 후 채택 안 함**
- "한 번에 2개씩 처리되게 할 수 있냐"는 질문에, 코드로 바로 만들기 전에
  이 컴퓨터의 실제 조건부터 확인: CPU 8코어, PyTorch가 수식 인식 한 번에
  기본으로 8스레드를 전부 씀. 이 상태에서 그냥 워커를 2개로 늘리면
  8코어에 16스레드가 몰리는 오버서브스크립션이 생길 것으로 예상됨.
- 실제 pix2text 모델로 독립 벤치마크 스크립트를 만들어 직접 측정
  (앱 코드는 건드리지 않음): 같은 문서에서 자른 실제 수식 이미지 6장을
  ① 워커 1개·스레드 8개·순차 처리, ② 워커 2개·스레드 4개씩·동시 처리
  두 방식으로 각각 처리해 총 소요 시간 비교.
- 결과: 순차 113.51초 vs 동시 130.69초 — **동시 처리가 오히려 15.1% 더
  느림** (개별 이미지 단위로 봐도, 동시 처리 쪽에서 순차 처리 최댓값보다
  더 오래 걸린 이미지가 나옴 — 스레드 경합으로 인한 역효과가 실측으로
  확인됨). 이 하드웨어/모델 조합에서는 지금의 단일 워커·풀스레드 방식이
  이미 더 나은 선택이라는 결론을 내리고, 코드 변경 없이 유지.

---

## 11. OCR 인식 파이프라인 근본 재설계 - 문자 분류에서 레이아웃 분석 기반 통짜 인식으로 (2026-08-07)

`839d85d` Rebuild OCR pipeline around layout analysis instead of PDF text-layer classification

**발단: "OCR 결과가 형편없다"**
- 사용자가 리뷰 화면에서 결과를 보다가 지적. 실제 문제로 파고들어 보니
  단순한 버그가 아니라 이 PDF 자체의 성격 문제였음을 확인: 페이지 전체가
  벡터 그림 하나 없이 통짜 JPEG(스캔본)인데, 그 위에 어떤 외부 도구가
  미리 심어놓은 부정확한 OCR 텍스트 레이어가 얹혀 있었음(원문자 선택지
  마커 ①~⑤가 "CD", "@", "(5)" 같은 엉뚱한 글자로 박혀 있는 등). 기존
  코드는 "텍스트 레이어 유무"만으로 네이티브 PDF와 스캔 PDF를 나눴는데,
  이 문서는 그 구분 자체가 성립하지 않는 제3의 유형이었음.

**1차 버그 수정 (문자 분류 파이프라인 안에서)**
- `merge_segments`가 문자 *타입*만 보고 세그먼트를 이어붙여서, 폰트
  인코딩이 깨진 원문자 마커 때문에 문제 본문 마지막 조각이 훨씬 아래에
  있는 무관한 선택지 행까지 하나로 뭉쳐지는 버그 발견 → 세로로 같은 행에
  있을 때만 잇도록 `_chars_same_row` 조건 추가.
- `cluster_formula_segments`의 높이 비례 간격 허용치가, 폭이 넓은
  선택지 행 자체의 큰 높이 때문에 그 위 문제 본문까지 이어붙이는 것도
  발견 → 세로로 전혀 안 겹치는 두 조각은 작은 고정 하한값만 적용하도록
  `boxes_close` 수정.

**`backend/ocr.py`로 분리 + 한글 주석 재작성**
- app.py에서 OCR 인식 로직만(문자 분류/클러스터링/LaTeX 정리/선택지
  복구/DB 저장 진입점) 별도 파일로 분리 - Flask 라우트, OCR 작업 큐와
  분리해서 인식 파이프라인만 따로 디버그하기 쉽게 함.
- 이후 요청으로 모든 주석을 `#` 스타일 + 한글로 재작성(docstring 전부
  변환). 로직은 한 글자도 안 바꿈.

**근본 재설계 (plan mode로 조사 후 구현)**
- "OCR 방식 자체를 제대로 고민해볼 필요가 있다"는 문제 제기로 plan mode
  진입. 실제 문서를 페이지별로 뜯어보며 텍스트+수식, 선택지, 그림, 보기/
  조건 박스, 표까지 다양한 콘텐츠 유형을 확인.
- 설치된 `pix2text` 라이브러리에 지금까지 안 쓰던 `recognize_page()`
  (전체 페이지 레이아웃 분석, TEXT/FORMULA/FIGURE/TABLE 타입 분류 +
  표는 셀 단위 bbox까지 제공)가 있는 걸 발견하고 실제 문제로 직접
  테스트 - 표/그림 자동 검출이 실제로 잘 작동하는 것 확인.
- 1차 구현: `analyze_problem_layout`으로 레이아웃을 먼저 뽑고 영역
  타입별로(그림→크롭만, 표→셀별 재귀 인식, 수식→단독 재인식, 텍스트→
  기존 PDF 텍스트 레이어 파이프라인 재사용) 나눠 처리하는 2단계 구조로
  교체. 새 `type="table"` 콘텐츠(JSON: rows/cols/cells) 추가하고
  `ContentRow.vue`(셀 그리드 편집 UI)·`ProblemPrintout.vue`(실제
  `<table>` 렌더링)에 반영.
- 검증 중 레이아웃 인식 자체의 한계 두 가지를 추가로 발견해 보정:
  ① 신뢰도 낮은 영역(옆으로 넓고 성긴 선택지 행)을 통째로 놓치는 경우
  발견 → 어느 영역에도 안 걸리는 빈 구간을 찾아 텍스트 영역으로 보정하는
  `_fill_layout_gaps` 추가. ② 문장 맨 앞에 수식이 바로 붙는 줄을 통째로
  FORMULA로 잘못 분류해서 단일 수식 OCR에 넣었다가 환각을 일으키는 경우
  발견 → 해당 영역에 한글이 있으면 텍스트 경로로 강등하는
  `_region_contains_hangul` 추가.
- 문제 번호("10.", "29.") 자동 필터링 요청 → 숫자로 시작+마침표류
  문자+본문보다 큰 폰트를 조건으로 하는 `_strip_problem_number` 구현.
  검증 중 분수 분자처럼 기준선 위로 뜬 글자가 PDF 추출 순서상 문제
  번호보다 먼저 나오는 경우를 발견해, 맨 앞 글자만 보는 대신 영역 시작부
  20글자 안에서 패턴을 찾아 그 자리만 정확히 도려내는 방식으로 재구현.

**실측 비교 → 표만 남기고 나머지는 전부 통짜 인식으로 전환**
- "bbox 경계 다 무시하고 텍스트 레이어 없는 것처럼 통짜로 돌려보면
  어떠냐"는 요청으로, 서로 다른 페이지 3문제(그림+선지, 선지만, 표)를
  레이아웃 기반 방식과 통짜 스캔 방식(`recognize_scanned_problem_region`)
  양쪽으로 돌려 실제 비교. 결과: 선지 인식에서 통짜 방식이 압도적으로
  우수(예: 선지 5개 중 1개만 건지던 문제가 통짜 방식에서는 5개 다 정확).
  표는 반대로 통짜 방식에 구조 정보가 아예 없어(행/열 없이 값만 죽
  나열) 표는 레이아웃 분석이 필수라는 결론.
- 이 실측 결과에 따라 아키텍처를 다시 바꿈: **표만** `analyze_problem_layout`
  으로 구조화하고, 표/그림 영역은 흰색으로 마스킹한 뒤 **나머지 전부**
  (본문/수식/선지)는 문제 크롭 전체를 한 덩어리로 `_recognize_pixel_region`
  (pix2text 레이아웃 인식 + EasyOCR)에 통짜로 넘기는 구조로 전면 교체.
  마스킹은 표 셀 값이 통짜 스캔 결과에도 중복으로 나오는 걸 실제로
  확인하고 추가함.
- 이 교체로 PDF 텍스트 레이어 문자 분류 경로 전체(`classify_char_type`,
  `collect_ordered_chars`, `merge_segments`, `_chars_same_row`,
  `cluster_formula_segments`, `apply_latex_to_formulas`,
  `split_multi_choice_latex`, `_strip_problem_number`와 관련 상수들,
  `_extract_text_region`/`_extract_formula_region`)이 전부 안 쓰이게
  돼서 제거 - `ocr.py`가 1250줄에서 797줄로 줄어듦. 오늘 처음에 고쳤던
  두 버그(`_chars_same_row`, `boxes_close`)도 이 경로와 함께 사라졌지만,
  더 나은 방식(통짜 인식)으로 대체됐으므로 문제 없음.
- 읽기 순서 버그를 하나 더 발견: 표/그림 세그먼트와 통짜 스캔 결과를
  합친 뒤 최종적으로 `(bbox_y, bbox_x)` 단순 정렬을 쓰고 있었는데,
  지수/분수처럼 키가 큰 수식 조각은 같은 줄에 있어도 위쪽 y가 옆의
  평범한 텍스트보다 작게 잡히는 경우가 흔해서 같은 줄 안의 좌→우 순서가
  깨지는 걸 스크린샷으로 직접 지적받고 확인. 이미 만들어져 있던
  (하지만 마지막 병합 단계에서는 안 쓰이던) `_reading_order`(세로로
  겹치는 조각끼리 먼저 "같은 줄"로 묶은 뒤 그 안에서만 x 정렬)로
  교체해서 해결.

**`clean_latex` 후처리 규칙 추가**
- `\stackrel{X}{=}` / `\stackrel{=}{X}` (등호 위·아래에 뭔가 얹힌 형태) →
  등호 하나만 남기도록 `STRAY_EQUALS_STACKREL_RE` 추가.
- 이 앱이 다루는 문제 지문에는 볼드체가 원래 없다는 지적에 따라,
  `\mathbf`/`\boldsymbol`/`\textbf`/`\bold{X}`는 명령어만 벗겨 `X`만
  남기고(중첩까지 반복 처리), 옛 스타일 선언형 `{\bf ...}`는 `\bf`
  토큰만 제거하는 규칙 추가.

**재OCR("Re-run OCR") 버튼을 OCR 큐로 전환**
- 재OCR을 누른 뒤 다른 문제로 넘어가면 버튼이 계속 비활성 상태로 남는
  버그 발견 - 기존엔 `/api/problems/<id>/recognize`가 요청 안에서 동기로
  OCR을 돌려서(수 분 소요 가능), 버튼의 로딩 상태가 그 요청 하나에만
  묶인 로컬 변수였음.
- OCR 작업 큐에 `"problem"`이라는 새 작업 종류 추가(`_run_problem_recognition_task`).
  엔드포인트는 이제 문제 상태를 `pending`으로 바꾸고 큐에 등록만 한 뒤
  즉시 응답. 프론트 버튼은 로컬 변수 대신 폴링돼 오는
  `problem.status === 'pending'`을 기준으로 비활성화 - 다른 문제로
  넘어가도 정확한 상태 유지. 이미 있던 폴링 로직
  (`stillRecognizing`/`startPollingIfNeeded`, 최초 인식 단계에서 쓰던 것)을
  그대로 재사용.
- 이 김에 "지금 OCR 진행 중인지 전혀 분간이 안 간다"는 요청으로
  `GET /api/ocr/status`(현재 처리 중인 작업의 문서명·페이지, 대기 개수)를
  새로 만들고, 상단 탭 바에 2초 간격으로 폴링하는 상태 표시(파란 점
  깜빡임 + "OCR: 파일명 p.N (+N queued)")를 추가 - "problem" 작업도
  동일하게 여기 반영됨.
- 실제 검증: 응답 시간 100~300초 → 0.07초로 즉시 반환, 처리 중
  `/api/ocr/status`에 `kind: "problem"`으로 표시되는 것 확인, 완료 후
  문제 상태가 `recognized`로 정상 전환되고 상태 표시도 idle로 복귀하는
  것까지 실측.

**변경 파일**
- `backend/app.py`, `backend/ocr.py`(신규)
- `frontend/src/App.vue`, `frontend/src/components/ContentRow.vue`,
  `frontend/src/components/ProblemPrintout.vue`,
  `frontend/src/components/ProblemReview.vue`

---

## 12. 선지 행이 그림으로 오인식되는 문제 수정, 백엔드 생존 여부 표시 추가 (2026-08-07)

`f015aad` 커밋.

**선지 행("① 3/2 ② 5/2 ...")이 통째로 이미지로 저장되는 버그**
- 스크린샷으로 "선지를 이미지로 잘못 인식한다"는 제보를 받고, DB에서
  최상위(부모 없는) `type='image'` 콘텐츠를 뽑아 실제로 크롭 파일을
  열어보는 방식으로 재현 사례를 확보 - `bbox_w=436, bbox_h=45`짜리
  가로로 길고 얇은 크롭을 열어보니 정확히 그 선지 다섯 개 줄이었음.
  `analyze_problem_layout`(pix2text `recognize_page`)이 이 영역을
  `ElementType.FIGURE`로 잘못 분류해서, `_extract_figure_region`이
  OCR을 아예 안 하고 이미지 블록으로만 저장하고 있었음.
- 처음 시도한 해법("의심되는 FIGURE 영역을 다시 픽셀 스캔해서, 텍스트/
  수식으로 덮이는 비율이 높으면 그림이 아니라고 뒤집는다")은 실측해보니
  거꾸로였음: 진짜 도형(그래프, 삼각형)도 곡선/선 전체를 감싸는 박스
  하나로 검출돼서 오히려 커버리지가 100%에 가깝게 나옴(도형 3개 실측:
  0.85~0.99). 반면 진짜 선지 행 중 하나는 크롭이 너무 얇아서(27px)
  검출 모델이 아예 박스를 하나도 못 찾는 경우도 발견(커버리지 0).
  → 이 방식은 폐기.
- 대신 **모양 기반 필터**로 교체: FIGURE 영역이 짧고(`h ≤ 70px`)
  넓적하면(`w ≥ 4h`) 선지 행 후보로 보고, 위아래에 흰 여백(20px)을
  둘러서 다시 스캔(`_figure_region_is_actually_text`) - 여백을 주니
  27px짜리 얇은 크롭도 정상적으로 5개 박스로 검출됨. 실제 도형 3개는
  전부 `h`가 150px 이상이라 이 단계에서 애초에 걸러지지 않고 그림으로
  남음 - 임계값 사이 여유가 충분히 커서 오탐 위험은 낮다고 판단.
- 재스캔 결과가 실제로는 두 가지 다른 방식으로 망가져 있는 걸 확인,
  둘 다 별도로 복구:
  1. 좁은 크롭 하나를 통째로 스캔하면 pix2text가 다섯 선지를 낱개로
     안 쪼개고 `\qquad`로 구분된 수식 하나에 다 욱여넣음(원문자 마커는
     `\oplus` 등 무관한 기호로 오독). `expand_choice_glob_segments`를
     추가해 `\qquad` 기준으로 쪼개고 조각별 leading 명령어 토큰(마커)을
     벗겨냄.
  2. 다른 경우엔 낱개로는 잘 쪼개졌지만(EasyOCR 텍스트 경로), 원문자
     마커가 "0", "@" 같은 전혀 무관한 글자로 읽혀서, 기존
     `relabel_choice_row`가 요구하는 "이미 제대로 읽힌 choice 하나"라는
     발판(anchor)이 문제 전체에 하나도 없어 선지로 인정을 못 받음.
     `_rescue_unclaimed_figure_rows`를 추가해, "그림으로 오판될 만큼
     짧고 넓적한 영역에서 나왔다"는 출처 자체(`_from_figure_row` 플래그)를
     충분한 증거로 보고 마커 인식 여부와 무관하게 위치 순으로 강제
     ①②③④⑤ 라벨링.
- 실제 DB에 있던 두 사례(스크린샷 속 문제 포함, 문제 ID
  `859454293b0d45f08f4d0e9c720aadcd`)로 전체 파이프라인을 직접 돌려
  검증 - 둘 다 개별 선지로 정상 분리되고 실제 도형 3개는 여전히
  이미지로 유지되는 것 확인.
- 알려진 잔여 한계: 마커가 숫자로 오독된 경우(예: "0 117") 값 앞에
  그 "0 "이 그대로 남는다 - 마커-숫자-오독과 "11"이 "1 1"로 읽히는
  정상적인 OCR 공백 아티팩트를 코드만으로 안전하게 구분할 방법이 없어,
  억지로 벗겨내다 정상 값을 훼손할 위험이 더 크다고 보고 그대로 둠
  (검토자가 수동으로 정리).

**메인 화면에 백엔드 생존 여부 표시 추가**
- 계기: "웹 업로드가 안 된다"는 문의로 조사해보니, 프론트(Vite, 5173
  포트)는 떠 있는데 백엔드(Flask, `app.py`, 5000 포트)가 꺼져 있었음
  - 떠 있던 `python.exe` 프로세스 2개는 이 프로젝트와 무관한
  `Desktop\collector\main.py`였던 것으로 확인, 백엔드가 언제/왜
  내려갔는지는 알 수 없었지만 프론트가 살아있으니 화면상으로는
  아무 이상 신호가 없어서 원인 파악이 오래 걸림.
- 재발 시 바로 알아챌 수 있도록, 이미 있던 OCR 상태 폴링(2초 간격
  `fetchOcrStatus`)에 얹어서 "Backend online/offline" 표시 추가 - 새
  타이머나 새 엔드포인트 없이, 기존 폴링이 실패하면(`catch` 블록)
  `backendOnline`을 false로 뒤집는 것만 추가. 상단 탭 바에 OCR 상태
  표시 왼쪽에 위치, 오프라인일 때 빨간 점이 깜빡임.

**변경 파일**
- `backend/ocr.py`, `frontend/src/App.vue`

---

## 13. MFR(수식 인식) 속도 개선 - DirectML/NPU/KV캐시 검토 후 INT8 양자화로 결론 (2026-08-07)

`6b2a85a` 커밋.

**출발점**: OCR 파이프라인에서 어느 단계가 제일 오래 걸리는지 물어봐서 단계별로 실측 -
레이아웃 분석(DocLayout-YOLO) ~1.7초, MFD(수식 위치 검출) 0.15~1.6초인데 반해,
**MFR(수식→LaTeX, pix2text-mfr-1.5)만 박스 하나당 4~7초(첫 호출은 최대 25초)**로
압도적으로 느림. 원인을 config까지 열어서 확인 - TrOCR 구조(DeiT 인코더 + BART류
디코더)를 ONNX로 export하면서 `use_cache: false`로 배포됨 - 캐시 없이 토큰 하나 낼
때마다 지금까지 나온 시퀀스 전체를 처음부터 다시 계산(사실상 O(N²)).

**시도 1: DirectML (Intel Arc 130V GPU)**
- `onnxruntime` → `onnxruntime-directml`로 교체, pix2text의 `more_model_configs={'provider':
  'DmlExecutionProvider'}`로 MFR만 GPU에 태워봄 - MFR 단독은 CPU 대비 ~2.1배,
  MFD는 그대로 CPU에 남아(ultralytics가 DirectML을 인식 못 함) 실제 운영 경로
  (MFD+MFR) 기준으로는 ~1.4배.
- 치명적 부작용 발견: `onnxruntime-directml` 설치 후 MFD(ultralytics)가 자기
  의존성 검사에서 "onnxruntime을 못 찾았다"고 판단해 **백그라운드에서 몰래 일반
  onnxruntime을 재설치**하려 시도 - 실제로 걸려서 설치 상태가 뒤섞이는 걸 확인.
  `ULTRALYTICS_SKIP_REQUIREMENTS_CHECKS=1`로 막을 수 있는 걸 찾았지만, 얻는
  이득 대비 이 취약성이 부담스러워서 보류하기로 결정하고 `onnxruntime`을 원래
  버전(1.28.0, CPU)으로 되돌림 - `ocr.py`에는 반영 안 함.

**시도 2: Intel NPU** — 하드웨어는 있음(Core Ultra 5 226V, "Intel(R) AI Boost" 확인).
다만 NPU는 고정 입력 크기에 강한데, 느린 건 정확히 그 반대인 가변 길이
autoregressive 디코더라 적합하지 않다고 판단, 실측 없이 기각.

**시도 3: KV 캐시 활성화** — `decoder_model.onnx`를 직접 열어 확인해보니 애초에
`past_key_values` 입출력이 없는(캐시 미지원) 그래프로 export되어 있어서 설정만
바꿔서는 안 됨. pix2text가 pytorch 백엔드로 쓰는 체크포인트
(`breezedeus/pix2text-mfr-1.5-pytorch`)를 로드해서 캐시가 되는 순정 HF
`generate()`를 쓰려 했으나 **HF 저장소가 401(비공개/인증 필요)**로 막혀 있음 -
공개된 ONNX 모델 카드의 공식 예제 코드 자체가 `use_cache=False`를 쓰고 있어서,
제작자가 캐시 지원 버전을 따로 공개하지 않은 것으로 보임. 남은 방법(ONNX 안에
박힌 가중치를 텐서 이름 대조로 PyTorch에 역매핑해서 재-export)은 잘못 매핑하면
"그럴듯하지만 미묘하게 틀린 LaTeX"을 조용히 낼 위험이 있어 보류.

**시도 4: INT8 동적 양자화 → 채택**
- `onnxruntime.quantization.quantize_dynamic`으로 기존 `encoder_model.onnx`/
  `decoder_model.onnx`를 그대로 양자화(가중치 재추출/재매핑 없음, 같은 가중치
  정밀도만 낮춤 - decoder 32MB→8.5MB, encoder 87.5MB→23MB).
- 정확도 검증: matplotlib mathtext로 실제 조판에 가까운 수식 이미지 10개(분수,
  지수, 적분, 시그마, 삼각함수 조합, 극한, 중첩 분수/근호 등)를 만들어 FP32와
  INT8 출력을 대조 - **10개 중 9개 완전 일치**, 나머지 1개도 `\frac{...}`를
  감싸는 불필요한 중괄호 하나 차이뿐이라 렌더링 결과는 동일. 실질적 정확도
  손실 없음으로 판단.
- 속도: 간단한 수식 4개 평균 **1.73배**, 복잡한 수식 6개 평균 **1.33배** 개선
  (CPU에서, 새 드라이버·패키지 충돌 없이).
- `ocr.py`에 `_get_quantized_mfr_dir()` 추가 - `%APPDATA%\pix2text`에 pix2text가
  이미 받아둔(또는 그 시점에 처음 받는) 원본 FP32 모델을 찾아 INT8 사본을
  한 번만 만들어 캐시해두고, 이후에는 그 사본 경로를 재사용한다(다른 지연 로딩
  싱글턴들과 같은 패턴). `get_formula_ocr_model()`이 `Pix2Text.from_config()`
  호출 시 `total_configs={'text_formula': {'formula': {'model_dir': ...}}}`로
  MFR만 이 양자화 사본을 쓰도록 지정 - 레이아웃/MFD/표 구조 모델은 그대로.
  캐시 없는 상태에서 첫 로드(양자화 포함) 18초, 이후에는 캐시 재사용으로
  일반 모델 로딩과 동일한 시간.

**변경 파일**
- `backend/ocr.py`

---

## 14. OCR 작업 시간 로깅 확충 + `recognize_page()`의 중복 인식 제거 (2026-08-07)

`426a83e` 커밋.

**OCR 큐 작업 시작/종료 타임스탬프**
- "지금 등록된 문제 2개 OCR이 얼마나 걸렸냐"는 질문에 서버 로그를 손으로
  역추적해야 했던 것을 계기로, `backend/app.py`의 `_ocr_worker_loop`(모든
  종류의 OCR 작업이 거쳐가는 단일 지점)에 시작/종료 로그 추가 -
  `[OCR] {kind} task started/finished in Xs: {task}` 형태로 `server.log`에
  바로 남음.

**`recognize_page()`가 버려지는 인식 결과를 만드느라 시간을 낭비하고 있던 문제**
- 파이프라인 각 단계(레이아웃 분석/그림·표 처리/통짜 스캔/선지 복구)에
  타임스탬프를 걸고 실제 등록된 문제 2개로 다시 분석하다가, "레이아웃 분석"
  단계가 그 자체 로그(예: `976.1ms`)와 무관하게 문제당 20~30초씩 걸리는
  이상한 현상을 발견.
- pix2text 소스(`pix_to_text.py`의 `recognize_page`)를 직접 읽어서 원인
  확인: `recognize_page()`는 단순 레이아웃(박스+타입) 검출기가 아니라,
  검출된 영역마다 **내용까지 전부 인식**(TEXT/TITLE 영역엔 MFD+MFR, TABLE
  영역엔 표 구조+셀 내용, FORMULA 영역엔 MFR)하는 함수였음.
  `analyze_problem_layout`은 그 결과에서 `type`/`box`만 쓰고 인식된 내용
  (`el.text`)은 전부 버리고 있었다 - 즉 **MFR을 문제당 사실상 두 번
  돌리고 있었다**(레이아웃 단계에서 한 번, 나중에 통짜 스캔에서 한 번).
- 직접 대조 실측: 같은 이미지에 `model.layout_parser.parse()`(레이아웃만)는
  2.28초, `model.recognize_page()`(내용까지)는 17.51초 - 반환하는 박스
  개수/타입은 동일(7개, TEXT/FORMULA/IGNORED). 차액이 전부 버려지는
  인식 결과를 만드는 데 쓰이고 있었음이 확인됨.
- **수정**: `analyze_problem_layout`이 `recognize_page()` 대신
  `model.layout_parser.parse()`를 직접 호출하도록 교체. TABLE 영역만
  셀 구조가 필요해서 예외적으로 `model.table_ocr.recognize(crop,
  out_cells=True, ...)`을 그 영역에 한해 직접 호출 - `recognize_page()`와
  동일하게 10px 여백을 두고 크롭한다(셀 bbox 좌표계가 이 확장된 크롭
  기준이라 여백을 안 맞추면 셀 위치가 어긋남). `_extract_table_region`의
  첫 인자를 `Element` 객체 대신 `table_ocr.recognize()`가 반환한 dict를
  바로 받도록 정리(`element.meta` → `table_meta`로 이름도 변경).
- **검증**: 실제 등록된 문제 2개, 순수 인식 시간(모델 워밍업 제외) 기준
  175.93초 → 142.36초 (**약 19% 절감, 33.57초**). 세그먼트 개수(19개,
  45개)는 수정 전후 정확히 동일 - 인식 결과 변화 없이 낭비만 제거.
  표(TABLE) 경로는 지금 등록된 문제에 표가 없어서 이번 실측으로는
  검증 못 함 - `recognize_page()` 내부 코드를 그대로 옮긴 것이라 논리적으로는
  동일해야 하지만, 실제 표 있는 문제로는 아직 미확인.
- 조사 과정과 다른 시도(DirectML/NPU/KV캐시/양자화/배치)까지 포함한 전체
  기록은 `speed_optimization.md`에 정리.

**변경 파일**
- `backend/app.py`, `backend/ocr.py`

---

## 15. OCR 파이프라인 전체 재점검 - 사소한 정리 하나만 반영 (2026-08-07)

`3472595` 커밋.

이전 커밋(레이아웃 단계 중복 인식 제거) 이후, "중복된 프로세스가 더
있는지" 요청으로 파이프라인 전체(모델 로딩/레이아웃 분석/그림·표 처리/
통짜 스캔/선지 복구/저장)를 한 번 더 꼼꼼히 훑음.

- `table_ocr`(Table Transformer)이 표 없는 문서에서도 무조건 로딩되는 것
  발견(실측: `enable_table=False` 5.28초 vs `True` 9.07초, 차이 3.79초) -
  다만 서버 프로세스당 단 한 번(이후 모든 요청에 상각)이라 lazy-loading의
  복잡도 대비 이득이 안 맞다고 판단해 보류.
- `analyze_problem_layout`이 `model.layout_parser.parse()`를 부르기 전에
  하던 `img.copy()`가, `parse()` 내부에서 자체적으로 `img.convert("RGB")`로
  또 사본을 만들기 때문에 완전히 버려지는 작업이었던 것을 발견 - 시간상
  의미는 거의 없지만(작은 크롭 이미지 복사) 제거.
- `recognize_scanned_problem_region` 함수가 `app.py` 어디서도 호출되지
  않는 죽은 코드인 것 확인 - 삭제 요청은 없었으므로 그대로 둠, 기록만 남김.
- 그림 후보 영역마다 MFD(수식 위치 검출)를 별도로 한 번씩 더 돌리는 것도
  확인했으나, MFD 자체가 원래 저렴(0.3~2초)해서 통합 리팩터링 비용 대비
  얻을 게 적다고 판단해 보류.
- 전체 조사·판단 기록은 `speed_optimization.md`에 정리.

**변경 파일**
- `backend/ocr.py`, `speed_optimization.md`

---

## 16. 선지 인식 실패 두 종류 수정 - qquad 정규식 완화, array 표 환각 분할 재시도 (2026-08-08)

`3cd239a` 커밋.

지금 등록된 실제 문제 2개의 OCR 결과를 검토하다가, 두 문제 모두 선지가
전혀 개별 항목으로 분리되지 않고 있는 걸 발견 - 원인을 파보니 서로
다른 두 가지 실패였다.

**1. `expand_choice_glob_segments`가 마커 없는 첫 조각 때문에 통째로 포기하던 버그**
- 문제 1의 선지 뭉치: `- 1 \qquad\textcircled{1} \ -\frac{3}{4} \qquad...`
  - 첫 조각("- 1")에 마커 명령어가 없음(그 선지의 마커가 별도 텍스트
    세그먼트 "0"으로 떨어져 나가 있었음). 기존 코드는 "조각 전부가
    마커로 시작해야 한다"는 조건이라 하나라도 안 맞으면 통째로 원본
    그대로 저장했음.
  - 수정: 과반수만 마커로 시작하면 선지 뭉치로 인정하고, 마커 없는
    조각은 내용을 그대로 값으로 쓴다(반대로 과반 미만이면 여전히
    손대지 않아 일반 수식에 우연히 낀 `\qquad`를 잘못 건드릴 위험은
    피함). 마커 뒤에 `\textcircled{1}`처럼 중괄호 인자가 붙는 경우
    그 인자까지 같이 벗겨내지 못하던 것도 같이 고침.
  - 검증: 실제 문제 1로 재실행 - 선지 없음 → `①②③④⑤` 5개로 정상 분리
    (다만 `①` 값이 "0"으로 나오는데, 원래 이게 `②`의 마커 오독 잔재일
    가능성이 있어 보임 - 완벽하진 않지만 이전보다 훨씬 나음).

**2. MFR이 선지 줄을 표(array)로 착각해 환각을 일으키는 문제**
- 문제 2의 선지 줄이 `\begin{array}{c c c c...}` 안에 `\textcircled`/
  `\oplus` 잡음 기호 수십 개가 뒤섞인 형태로 나오고 있었음 -
  `mfr_batch_size` 실험 때 같은 박스에서 봤던 그 환각과 동일.
- 이 앱의 문제 지문/선지에는 array/matrix 서식이 정상적으로 나올 일이
  없다(진짜 표는 TABLE 레이아웃 경로로 따로 처리됨)는 점에 착안해,
  `_recognize_pixel_region`이 만든 formula 세그먼트에 `\begin{array}`가
  섞여 있으면 사실상 환각으로 간주하고 처리하는 `_fix_array_hallucinations`/
  `_retry_split_region`을 추가. 같은 크롭을 그대로 다시 넣으면 모델이
  결정론적이라 의미가 없으므로, 영역을 좌우로 반씩 쪼개서 각각 따로
  재인식한다(선지 여러 개가 한꺼번에 보이는 게 환각 원인으로 보여서,
  한 번에 보여주는 항목 수를 줄이는 접근 - `_figure_region_is_actually_text`가
  "좁게 잘라서 다시 보여주면 더 잘 읽는다"는 걸 이미 확인한 것과 같은
  원리). 재시도 결과에도 환각이 남아있으면 원본을 그대로 둔다(이미
  망가진 값이라 더 나빠질 것 없음). 이전에 검토했던 "MFR 배치 크기를
  전체적으로 키우는" 방법은 멀쩡한 다른 결과까지 예측 불가능하게 바뀌는
  위험 때문에 보류했었는데, 이번 방식은 **이미 환각으로 확인된 것만**
  건드리므로 그 위험을 피한다.
- 검증: 실제 문제 2로 재실행 - `\begin{array}` 환각 완전히 사라지고
  `①-⑥` 6개 선지로 분리됨(5개여야 하는데 6개가 나온 것과 `④`의 부호
  누락은 분할 경계가 하필 애매한 지점을 잘라서 생긴 잔여 흠으로 보임 -
  그래도 통짜 표 환각보다는 훨씬 검토하기 쉬운 상태).

**3. (과정에서 발견) `\mathbf{}` 볼드 제거가 중첩 중괄호에서 실패하던 버그**
- 위 2번 검증 중 `\mathbf{-\frac{2}{3}}`처럼 볼드 인자 안에 `\frac{2}{3}`
  같은 중첩 중괄호가 있으면 기존 `BOLD_WRAPPER_RE`(`[^{}]*`, 중괄호
  전혀 허용 안 함)가 매칭 자체를 못 해서 볼드가 그대로 남는 것 발견.
  중괄호 없는 부분과 한 겹짜리 중첩 그룹을 번갈아 반복 매칭하도록
  정규식을 고쳐서 해결.

**변경 파일**
- `backend/ocr.py`

---

## 17. `clean_latex` 후처리 규칙 3개 추가 - \root\of, 빈 밑줄/윗줄, \hfill (2026-08-08)

`53ae4af` 커밋.

새로 업로드된 문서를 검토하다가 리뷰 화면에서 KaTeX가 렌더링 못 하고
빨간 글씨로 그대로 노출하는 명령어들을 추가로 발견해서 각각 후처리
규칙을 추가했다.

- **`\root n \of x` (plain TeX 어근 표기)**: KaTeX는 `\root`/`\of`를
  아예 모르는 명령이라 렌더링이 깨짐(실제 사례: `\root3 \of{2}`,
  `\root4 \of{8}`, `2 \root4 \of{2}`). KaTeX가 지원하는 `\sqrt[n]{x}`로
  변환하는 `ROOT_OF_RE` 추가 - 인덱스/피개근수 둘 다 중괄호로 감싸져
  있든 아니든 처리.
- **빈 `\underline{}`/`\overline{}`**: 밑줄/윗줄(선분 표기 등) 칠 내용을
  OCR이 놓치고 빈 명령어만 남기는 경우가 있음. 기존의 "빈 중괄호 그룹
  제거" 규칙은 중괄호만 지우고 명령어 자체는 인자 없이 남겨서 오히려
  렌더링이 깨졌는데, `EMPTY_LINE_WRAPPER_RE`로 명령어까지 통째로 제거.
- **`\hfill`**: 케이스별 정의식(`\begin{matrix}...`) 각 줄을 오른쪽으로
  밀어 정렬하던 원본 조판의 TeX 레이아웃 명령이 수식 내용과 함께 그대로
  섞여 나온 것(실제 사례: `a_{n}\!+\!1~~(a_{n}<3)\hfill`) - 수식 내용이
  아니라 순전히 간격 조정용이라 기존 `JUNK_LATEX_COMMANDS` 목록에 추가
  해서 다른 환각 토큰들과 같은 방식으로 제거.

**변경 파일**
- `backend/ocr.py`

---

## 아직 처리 안 된 것으로 남아있는 이슈

- 어떤 문제의 수식에서 `a \stackrel{2}{\iota} = 0` 형태 발견 — `a^2 = 0`이
  잘못 인식된 것으로 추정되나 미확인 (fix.md에도 기록됨).
