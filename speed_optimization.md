# OCR 속도 개선 조사 정리 (2026-08-07)

MFR(수식 인식) 단계가 OCR 파이프라인에서 압도적으로 느리다는 문제를 붙잡고,
여러 방향을 실측 기반으로 검토한 기록. 최종 채택 여부와 함께, 안 되거나
보류한 방향도 나중에 다시 안 건드리도록 이유를 남겨둔다.

---

## 0. 출발점 - 병목 진단

단계별로 실측한 결과:

| 단계 | 모델 | 소요 시간 |
|---|---|---|
| 레이아웃 분석 | DocLayout-YOLO | ~1.7~2.2초 (문제당 1회) |
| 수식 위치 검출 (MFD) | Pix2Text-MFD-1.5 (YOLO) | 0.15~2초/호출 |
| **수식→LaTeX (MFR)** | **Pix2Text-MFR-1.5 (TrOCR)** | **박스당 4~7초(첫 호출 최대 25초)** |
| 한글 텍스트 인식 | EasyOCR | 1초 미만/박스 |

**근본 원인**: MFR은 TrOCR 구조(DeiT 인코더 + BART류 디코더)를 ONNX로 export한
것인데, `config.json`/`generation_config.json`에 `use_cache: false`가 박혀
있음 - 캐시 없이 토큰 하나 낼 때마다 지금까지 나온 시퀀스 전체를 처음부터
다시 계산(사실상 O(N²)). 모델 카드의 공식 예제 코드 자체가
`ORTModelForVision2Seq.from_pretrained(..., use_cache=False)`를 쓰고 있어서,
제작자가 의도적으로 이렇게 배포한 것으로 보임(캐시 지원 버전은 따로 없음).

---

## 1. DirectML (Intel Arc 130V GPU) — 기각

- `onnxruntime` → `onnxruntime-directml`로 교체, MFR만 `DmlExecutionProvider`로
  돌려봄.
- MFR 단독은 CPU 대비 ~2.1배 빠르지만, MFD(ultralytics)는 DirectML을 인식 못
  해서 그대로 CPU에 남음 - 실제 운영 경로(MFD+MFR) 기준으로는 ~1.4배.
- **부작용**: `onnxruntime-directml` 설치 후 ultralytics가 자기 의존성 검사에서
  "onnxruntime을 못 찾았다"고 판단해 **백그라운드에서 몰래 일반 onnxruntime을
  재설치**하려 시도함 - 실제로 걸려서 설치 상태가 뒤섞이는 걸 확인.
  `ULTRALYTICS_SKIP_REQUIREMENTS_CHECKS=1`로 막을 수 있지만, 번거로움.
- **INT8 양자화와 조합 시 (아래 3번 참고) 오히려 최대 11배까지 더 느려짐** -
  DirectML이 INT8 양자화 연산(QuantizeLinear/DequantizeLinear 등)을 잘 못
  받아서 매 디코딩 스텝마다 GPU↔CPU 변환 비용이 캐시 없는 구조 위에 추가로
  누적되는 것으로 추정.
- **결론**: 이득 대비 취약성(패키지 충돌)과 조합 리스크가 커서 기각. `ocr.py`에는
  반영 안 함, 환경도 원래(`onnxruntime` CPU 빌드)로 되돌림.

## 2. Intel NPU — 실측 없이 기각

- 하드웨어는 있음(Core Ultra 5 226V, "Intel(R) AI Boost" NPU 확인됨).
- NPU는 고정 입력 크기 워크로드에 강한데, 정작 느린 MFR **디코더**는 정확히
  반대(가변 길이 autoregressive, 스텝마다 시퀀스 길이가 달라짐) - 현재 세대
  NPU가 가장 취약한 유형의 작업. DirectML에서 이미 확인한 "인코더만 가속되고
  디코더는 그대로"인 패턴이 반복될 가능성이 높고, 같은 종류의 패키지 충돌
  (`onnxruntime-openvino`)도 재발할 것으로 예상.
- **결론**: 기대 이득이 DirectML보다도 작아서 실측 없이 기각.

## 3. KV 캐시 활성화 (근본 해결) — 막힘

- `decoder_model.onnx`를 직접 열어 확인 - `past_key_values` 입출력이 아예 없는
  (캐시 미지원) 그래프로 export되어 있어서, 설정값만 바꿔서는 캐시를 켤 수 없음.
- pix2text가 pytorch 백엔드로 쓰는 체크포인트(`breezedeus/pix2text-mfr-1.5-pytorch`,
  캐시가 되는 순정 HuggingFace `.generate()`를 쓸 수 있는 경로)를 시도했으나
  **HF 저장소가 401(비공개/인증 필요)**로 막혀 있음 - 다른 방법으로 접근 가능한
  공개 저장소(`breezedeus/pix2text-mfr-1.5`, ONNX 버전)는 정상 로드되는 것과
  대조해서 확인, 진짜로 이 pytorch 저장소만 막혀 있는 것이 맞음.
- 남은 방법(ONNX 안에 박힌 가중치를 텐서 이름 대조로 PyTorch에 역매핑해서
  캐시 지원 버전으로 재-export)은 매핑을 잘못하면 "그럴듯하지만 미묘하게 틀린
  LaTeX"을 조용히 낼 위험이 있어 보류.
- **결론**: 외부 저장소 접근 제한으로 막힘. 나중에 접근 권한이 생기거나
  대안 체크포인트를 찾으면 재검토 가치 있음.

## 4. INT8 동적 양자화 — 채택 ✅

- `onnxruntime.quantization.quantize_dynamic`으로 기존 `encoder_model.onnx`/
  `decoder_model.onnx`를 그대로 양자화. 가중치 재추출/재매핑 없음 - 같은
  가중치, 정밀도만 낮춤(decoder 32MB→8.5MB, encoder 87.5MB→23MB).
- **정확도 검증**: matplotlib mathtext로 실제 조판에 가까운 수식 이미지 10개
  (분수, 지수, 적분, 시그마, 삼각함수, 극한, 중첩 분수/근호 등)로 FP32와 INT8
  출력 대조 - **10개 중 9개 완전 일치**, 나머지 1개도 `\frac{...}`를 감싸는
  불필요한 중괄호 하나 차이뿐이라 렌더링 결과는 동일.
- **속도 (합성 수식)**: 간단한 4개 평균 1.73배, 복잡한 6개 평균 1.33배.
- **속도 (실제 등록된 문제 2개, 전체 파이프라인 기준)**:

  | | FP32 | INT8 | 배율 |
  |---|---|---|---|
  | 문제 1 (19개 세그먼트) | 86.67초 | 64.75초 | 1.34배 |
  | 문제 2 (45개 세그먼트) | 124.20초 | 112.50초 | 1.10배 |
  | 합계 | 210.88초 | 177.25초 | **1.19배** |

  실제 경로 기준 배율이 합성 벤치마크보다 낮은 이유: 레이아웃 분석/MFD/EasyOCR
  등 양자화 대상이 아닌 고정 비용이 섞여서 전체 배율이 희석됨.
- **구현**: `backend/ocr.py`의 `_get_quantized_mfr_dir()` - pix2text가
  `%APPDATA%\pix2text`에 받아둔(없으면 그 시점에 처음 받는) 원본 FP32 모델을
  찾아 INT8 사본을 한 번만 만들어 캐시(다른 지연 로딩 싱글턴과 같은 패턴).
  `get_formula_ocr_model()`이 `Pix2Text.from_config(total_configs={'text_formula':
  {'formula': {'model_dir': ...}}})`로 MFR만 이 사본을 쓰게 지정 - 레이아웃/
  MFD/표 구조 모델은 그대로.
- **커밋**: `6b2a85a` (본 변경) / `91667ac` (HISTORY.md 해시 기록) - `main`에 반영됨.

## 5. OCR 작업 시작/종료 타임스탬프 로깅 — 채택 ✅ (부수 작업)

- 소요 시간을 물어봤을 때 서버 로그에 작업 경계 표시가 없어서 타임스탬프를
  손으로 역추적해야 했던 것을 계기로 추가.
- `backend/app.py`의 `_ocr_worker_loop`(모든 종류의 OCR 작업이 거쳐가는 단일
  지점)에 시작/종료 로그 추가 - `[OCR] {kind} task started/finished in Xs: {task}`
  형태로 `server.log`에 바로 남음. 스키마 변경이나 새 엔드포인트 없이 print 두 줄.

## 6. `mfr_batch_size` (여러 수식 박스를 한 번에 배치 처리) — 보류 ⏸

- pix2text의 `recognize_text_formula(..., mfr_batch_size=N)`으로 감지된 여러
  수식 박스를 한 번의 모델 호출로 묶어서 처리 가능(기본값 1, 한 번에 하나씩).
- 실제 44박스짜리 무거운 문제로 실측:

  | batch_size | 소요 시간 |
  |---|---|
  | 1 (현재) | 60.4~76.8초 |
  | 4 | 389.5초 (**6.4배 더 느림**) |
  | 8 | 304.5초 (**5배 더 느림**) |
  | 50 (전체를 한 배치로) | 30.5~32.8초 (**~2.3배 빠름**) |

  중간 크기 배치(4, 8)는 오히려 훨씬 느려지는 함정이 있음 - 캐시 없는 구조에서
  배치 안에 하나라도 "어려운" 수식이 섞이면 그 배치 전체가 그 수식이 끝날
  때까지 발목 잡히는 것으로 추정. 쓴다면 반드시 "전체 개수보다 큰 값"으로만
  써야 함(중간값은 절대 안 됨).
- **치명적 문제**: bs=1과 bs=50의 출력을 44개 박스 전부 대조한 결과, **3개가
  달랐음**. 2개는 공백/중괄호 차이로 렌더링 결과는 동일해 무해하지만, 나머지
  1개는 완전히 다른 결과:
  - bs=1: `\begin{array}{c c c c...}` 형태의 거대한 표 구조 환각(잡음 기호
    수십 개가 뒤섞인 완전히 망가진 출력)
  - bs=50: `\oplus\ -\frac{3}{4} \qquad\oplus\ -\frac{5}{6} ...` - 선지 행처럼
    보이는, 더 말이 되는 결과(우연히 `expand_choice_glob_segments`가 처리
    가능한 패턴과 유사)
  - 이번엔 우연히 배치 쪽이 나았지만, **배치 크기가 모델의 실제 생성 결과
    자체를 바꾼다**는 것 자체가 문제 - 캐시 없는 디코더가 배치로 묶인 다른
    수식들과 계산이 얽히면서(패딩, 어텐션 마스크 등) 수치적으로 다른 경로를
    타는 것으로 추정되며, 항상 개선 방향이라는 보장이 없음.
- **결론**: INT8 양자화는 44개 중 44개가 사실상 동일(변경 없음에 가까움)했던
  반면, 배치는 "가끔 다른 결과"를 낸다는 게 질적으로 다른 위험 - 검토 도구
  특성상 사용자 판단이 필요하다고 보고 보류. `ocr.py`에 반영 안 함.

## 7. `recognize_page()`가 버려지는 내용까지 전부 인식하고 있던 문제 — 채택 ✅

- 파이프라인 각 단계에 타임스탬프를 걸고 실제 문제 2개로 재분석하다가,
  "레이아웃 분석" 단계가 그 자체 로그(`976.1ms`)와 무관하게 문제당
  20~30초씩 걸리는 이상한 현상을 발견.
- pix2text 소스(`pix_to_text.py`의 `recognize_page`)를 직접 읽어서 원인 확인:
  `recognize_page()`는 단순 레이아웃(박스+타입) 검출기가 아니라, 검출된
  영역마다 **내용까지 전부 인식**(TEXT/TITLE 영역엔 MFD+MFR, TABLE 영역엔
  표 구조+셀 내용, FORMULA 영역엔 MFR)하는 함수. `analyze_problem_layout`은
  그 결과에서 `type`/`box`만 쓰고 인식된 내용(`el.text`)은 전부 버리고
  있었음 - 즉 **MFR을 문제당 사실상 두 번 돌리고 있었음**(레이아웃 단계에서
  한 번, 나중에 통짜 스캔에서 한 번).
- 직접 대조 실측: 같은 이미지에 `model.layout_parser.parse()`(레이아웃만)는
  **2.28초**, `model.recognize_page()`(내용까지)는 **17.51초** - 반환하는
  박스 개수/타입은 동일(7개, TEXT/FORMULA/IGNORED).
- **수정**: `analyze_problem_layout`이 `recognize_page()` 대신
  `model.layout_parser.parse()`를 직접 호출하도록 교체. TABLE 영역만
  셀 구조가 필요해서 예외적으로 `model.table_ocr.recognize(crop,
  out_cells=True, ...)`을 그 영역에 한해 직접 호출(`recognize_page()`와
  동일하게 10px 여백을 두고 크롭 - 셀 bbox 좌표계가 이 확장된 크롭 기준이라
  여백을 안 맞추면 셀 위치가 어긋남).
- **검증**: 실제 등록된 문제 2개, 순수 인식 시간(모델 워밍업 제외) 기준
  175.93초 → **142.36초 (약 19% 절감, 33.57초)**. 세그먼트 개수(19개,
  45개)는 수정 전후 정확히 동일 - 인식 결과 변화 없이 낭비만 제거.
  ⚠️ 표(TABLE) 경로는 지금 등록된 문제에 표가 없어서 이번 실측으로는
  검증 못 함(`recognize_page()` 내부 코드를 그대로 옮긴 것이라 논리적으로는
  동일해야 하지만, 실제 표 문제로 아직 확인 안 됨).
- **커밋**: TODO(아래 참고).

---

## 요약

| 방법 | 상태 | 실측 배율 (실제 문제 기준) | 비고 |
|---|---|---|---|
| DirectML | 기각 | ~1.4배 (단독), INT8과 조합 시 최대 11배 **느려짐** | 패키지 충돌 취약성 |
| Intel NPU | 기각 (실측 안 함) | - | 워크로드 부적합 (가변 길이 디코더) |
| KV 캐시 재-export | 막힘 | - | 필요한 HF 저장소가 비공개(401) |
| **INT8 양자화** | **채택** | **1.19배** | 정확도 손실 사실상 없음, 커밋됨 |
| **`recognize_page()` → `layout_parser.parse()` 교체** | **채택** | **1.24배 (19% 절감)** | 버려지던 중복 인식 제거, 정확도 변화 없음 |
| 작업 타임스탬프 로깅 | 채택 | - | 부수 작업, 커밋됨 |
| `mfr_batch_size` | 보류 | ~1.2~2.3배 (문제 크기에 따라) | 출력 자체가 달라지는 위험, 사용자 판단 대기 |
